# Raw data

Place downloaded datasets here in the following structure:

```
data/raw/
├── liar/
│   ├── train.tsv
│   ├── valid.tsv
│   └── test.tsv
├── fakenewsnet/
│   ├── politifact_fake.csv
│   ├── politifact_real.csv
│   ├── gossipcop_fake.csv
│   └── gossipcop_real.csv
└── fakeddit/
    ├── train.tsv
    ├── validate.tsv
    └── test.tsv
```

Shared download link (fill in once someone uploads to team Drive): `<add link here>`

Do not commit these files — they're git-ignored on purpose. Everyone on the
team should download them independently or pull from the shared Drive link.
